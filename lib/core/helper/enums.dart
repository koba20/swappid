enum ButtonType {
  primary,
  secondary,
  outline,
}

enum MessageType {
  success,
  error,
  info,
}

enum Environment {
  development,
  staging,
  production,
}

enum AppBarType {
  type1,
  type2,
}

enum KAlignment {
  start,
  center,
  end,
}

enum InputStyleType {
  border,
  outline,
  underline,
  none,
}

enum Position {
  after,
  before,
  extended,
  none,
}

enum ErrorTypes {
  data,
  server,
  secket,
  handshake,
  socket,
  format,
  timeout,
  unknown
}

enum ApiStatus {
  success,
  failed,
}
